# Measured findings (data plane)

This file is **generated** from `artifacts/data_quality_report.json` after `prepare` + `data_quality_report`. Regenerate with:

```bash
python tools/prepare_data.py --root . --max-sft 2000 --max-dpo 2000   # or full export
python tools/data_quality_report.py --root .
python tools/render_measured_findings.py --root .
python tools/plot_qa_figure.py --root .
```

## Scope of these numbers

- They describe **exported JSONL** (after recipe filters), not raw Hub tables in full.
- With default caps in the command above, rows are the **first** passing examples in dataset order — not a uniform subsample. For publication-style stats, export the full split or add explicit shuffling in `prepare_data.py`.
- **recipe_id:** `default-v0`
- **git_commit** (from manifest at export): `0c35f61922a4a2f534cc4adf84444b5e9f14c40c`

## SFT export (`repro_sft`)

- **rows:** 2000
- **mean output chars:** 254.97
- **p50 / p95 output chars:** 155.0 / 735.1
- **approx. duplicate fraction (normalized hash):** 0.0000
- **mean trigram repetition score (outputs):** 0.0184

### Interpretation

- Near-zero **duplicate fraction** on this slice indicates Alpaca exports are diverse under the chosen normalization (not semantic dedup).
- **Trigram repetition** is a cheap proxy for templated or looped text in `output`; unusually high values warrant spot-checking.

## DPO export (`repro_dpo`, Ultrafeedback → flat pairs)

- **rows:** 2000
- **mean chosen / rejected chars:** 1279.60 / 1131.92
- **p50 length delta (chosen − rejected):** 58.5
- **fraction chosen longer than rejected:** 0.556
- **fraction with full shared prefix (up to shorter length):** 0.0075
- **approx. duplicate fraction:** 0.0000

### Interpretation

- **Chosen longer than rejected** ~>0.5 is common in helpfulness-style prefs but is **not** a quality guarantee — length can correlate with hedging.
- **Shared-prefix rate** near zero here suggests many pairs diverge early (different assistant continuations from the same user prompt) — consistent with how Ultrafeedback binarized pairs are structured after our last-assistant extraction.

## Figure

See [`docs/figures/data_qa_overview.svg`](figures/data_qa_overview.svg) (generated by `tools/plot_qa_figure.py`).

_`data_quality_report.created_at`: `2026-03-22T04:53:03.253388+00:00`_
